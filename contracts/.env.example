# PortX contracts — Sepolia testnet only (copy to .env, never commit .env)
#
# WARNING:
# - Never commit DEPLOYER_PRIVATE_KEY or real API keys
# - Testnet only — NOT audited — NOT for mainnet
# - Do not enable PortX live execution (ENABLE_LIVE_EXECUTION stays false in the app)

SEPOLIA_RPC_URL=
DEPLOYER_PRIVATE_KEY=
ETHERSCAN_API_KEY=
